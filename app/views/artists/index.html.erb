<ul>
  <% @artists.each do |a| %>
    <li><%= link_to a.name, aritst_path(a)%> <br>
        <%= a.song_count %> songs<br>
      <ul>
        <% @artist.songs.each do |s| %>
          <li><%= link_to s.title, song_path(s) %></li>
        <% end %>
        </ul>
      </li>
      <% end %>
    </ul>
